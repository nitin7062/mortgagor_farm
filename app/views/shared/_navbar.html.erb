<nav class="navbar navbar-expand-md navbar-dark bg" style="background-color: 			#3b5998;">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href="/">Mortage Farm</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0 nav justify-content-end">
            <li class="nav-item">
                a class="nav-link active text-white" aria-current="page" href="/"><i class="bi bi-house " style = "font-size: 2rem" ></i></a>
            </li>    
        </ul>
        <ul>
            <li class="navbar dropdown ms-auto mb-2 mb-lg-0">
                <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <% if user_signed_in? %>
                      <%=current_user.email%>
                    <% else %>
                      Toggle
                    <% end %>
                </a>
                <ul class="dropdown-menu">
                    <% if user_signed_in? %>
                        <%=button_to "Logout", destroy_user_session_path, method: :delete,class: "dropdown-item"%>
                    <li><%=link_to current_user.email, edit_user_registration_path(:id => current_user.id),class: "dropdown-item"%></li> 
                    <% else %>
                        <%=link_to "Login", new_user_session_path,class: "dropdown-item"%>
                       <%=link_to "Sign-UP", new_user_registration_path,class: "dropdown-item"%>
                    <% end %> 
                </ul>
            </li>      
        </ul>
    </div>
  </div>
</nav>